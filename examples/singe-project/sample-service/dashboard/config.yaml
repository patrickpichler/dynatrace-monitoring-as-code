configs:
- id: overview
  config:
    name: Overview
    parameters:
      owner: test.user
      zoneId:
        type: reference
        api: management-zone
        config: main
        property: id
    template: dashboard.json

